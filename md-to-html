#!/bin/sh
in=$1
out=$2
opts=

has() {
    head -n1 $in | grep "$@" >/dev/null
}

has toc && {
    opts="$opts --toc --template=toc-and-body"
}
has number-sections && {
    opts="$opts --number-sections"
}

pandoc -f markdown_github-hard_line_breaks --data-dir=./pandoc-data $1 -o $2 $opts
